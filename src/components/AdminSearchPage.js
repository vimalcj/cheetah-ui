import React, { useState, useRef } from "react";

const AdminSearchPage = () => {
  
    return (
        <div className="container">
        <header className="jumbotron">
          <h3>New page loaded</h3>
        </header>
      </div>
    );
  };
  
  export default AdminSearchPage;